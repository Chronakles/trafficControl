In this project I implemented a synchronized traffic control with locks in java.
Only one car at a time can pass the bridge, while others are waiting. 
Also they switch left and right, that is one on the left side passes the bridge than one on the right side and so on.